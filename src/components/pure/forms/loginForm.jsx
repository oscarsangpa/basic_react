import React, { useState } from 'react'

export const LoginForm = () => {

  const initialData = [
    {
      user: '',
      password: ''
    }
  ]

  const [data, setData] = useState(initialData);

  return (
    <div>L</div>
  )
}
